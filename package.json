{
  "name": "reactapp",
  "version": "1.0.0",
  "description": "portfolio work ",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node server.js",
        "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "Lauren Northcut",
  "license": "ISC",
  "engines": {
    "node": "8.9.2"
}
}
